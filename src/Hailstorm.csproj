<Project Sdk="Microsoft.NET.Sdk">

	<PropertyGroup>
		<TargetFramework>net48</TargetFramework>
        <LangVersion>11</LangVersion>
		<PathMap>$(MSBuildProjectDirectory)=\RainWorldMods\Hailstorm</PathMap>
    </PropertyGroup>
    
    <ItemGroup>
        <Reference Include="../lib/*.dll;../lib/*.pdb">
			<Private>false</Private>
        </Reference>
    </ItemGroup>
    
    <ItemGroup>
      <Folder Include="Creatures\Vultures\Raven\" />
    </ItemGroup>
    
    <Target Name="GenerateMod" AfterTargets="PostBuildEvent">
		<ItemGroup>
			<RemovePlugins Include="../mod/plugins/*.dll" />
			<RemovePdbs Include="$(OutDir)*.pdb" />
		</ItemGroup>
		
		<ItemGroup>
			<CopyPlugins Include="$(TargetPath)" />
			<CopyPdbs Include="$(OutDir)*.pdb" />
		</ItemGroup>
		
        <Delete Files="@(RemovePlugins)" />
        <Copy SourceFiles="@(CopyPlugins);@(CopyPdbs)" DestinationFolder="../mod/plugins/" />
    </Target>
    
</Project>
